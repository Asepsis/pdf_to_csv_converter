[package]
name = "pdf_to_csv_converter"
version = "0.1.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
pdf-extract = "0.6.4"
regex = "1"
clap = { version = "3.1", features = ["derive"] }
colored = "2"